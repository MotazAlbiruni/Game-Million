<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.motazalbiruni.milliongame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_game"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_game_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MillionGame">
        <activity
            android:name=".ui.PlayScreen"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.MillionGame.NoActionBar"/>
        <activity
            android:name=".ui.EditingQuestions"
            android:label="@string/title_activity_editing_questions"
            android:theme="@style/Theme.MillionGame.NoActionBar" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>